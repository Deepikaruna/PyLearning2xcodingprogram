api_urls = ("sdet.live", "awesome.com", "tta.com")
# Tuple is used if values does needs to be modified like in above example
# websites names no need to be changed, so we use tuple

print(api_urls[0])
print(api_urls[1])
print(api_urls[2])


